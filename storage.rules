rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    match /tenants/{tenantId}/tickets/{ticketId}/{allPaths=**} {
      // Only authenticated staff/admin can read/write ticket images
      allow read, write: if request.auth != null;
    }
  }
}
